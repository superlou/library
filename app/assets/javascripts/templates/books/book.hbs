{{#if isEditing}}
    <form>
        {{partial 'books/edit'}}
        <button {{action 'saveEdit'}}>Save</button>
        <button {{action 'cancelEdit'}}>Cancel</button>
    </form>
{{else}}
    <h1>{{full_title}}</h1>
    <p>Code: {{code}}</p>
    <p>Notes: {{notes}}</p>
    <p>Adult only: {{adult}}</p>
    <p>Stock: {{stock}}</p>
    <button {{action 'edit'}}>Edit</button>
    <button {{action 'delete'}}>Delete</button>
{{/if}}