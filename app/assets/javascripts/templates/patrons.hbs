<div class='col-md-3'>
    <ul>
        {{#each controller}}
            <li>{{#linkTo 'patrons.patron' this}}{{name}}{{/linkTo}}</li>
        {{/each}}
    </ul>
</div>

<div class='col-md-9'>
    {{outlet}}
</div>
