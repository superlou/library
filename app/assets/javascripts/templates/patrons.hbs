<div id='filter_panel'>
    <ul>
        {{#each controller}}
            <li>{{#linkTo 'patrons.patron' this}}{{name}}{{/linkTo}}</li>
        {{/each}}
    </ul>
</div>

<div id='item'>
    {{outlet}}
</div>
