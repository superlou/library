<div class='small-3 columns'>
    <ul>
        {{#each controller}}
            <li>{{#linkTo 'patrons.patron' this}}{{name}}{{/linkTo}}</li>
        {{/each}}
    </ul>
</div>

<div class='small-9 columns'>
    {{outlet}}
</div>
