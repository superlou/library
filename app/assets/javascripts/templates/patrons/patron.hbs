{{#if isEditing}}
    <form role='form' class='form-horizontal'>
        <h1>Edit {{name}}</h1>
        {{partial 'patrons/edit'}}
        <button class='btn btn-primary' {{action 'saveEdit'}}>Save</button>
        <button class='btn btn-default' {{action 'cancelEdit'}}>Cancel</button>
    </form>
{{else}}
    <div class='row'>
        <div class='col-md-8'>
            <h1>{{name}}</h1>
        </div>
        <div class='col-md-4'>
            <button class='btn btn-default' {{action 'edit'}}>Edit</button>
            <button class='btn btn-default' {{action 'delete'}}>Delete</button>
        </div>
    </div>

    <ul class='slug-line'>
        {{codeSlug code}}
        {{emailSlug email}}
        {{identitySlug gender adult}}
    </ul>

    {{#if notes}}
        <h2>Notes</h2>
        <p>{{notes}}</p>
    {{/if}}
{{/if}}