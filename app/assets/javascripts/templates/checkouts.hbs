<div class='small-3 columns'>
    <ul>
        {{#each controller}}
            <li>
                {{#linkTo 'checkouts.checkout' this}}
                    {{book.title}}, {{patron.name}} [{{status}}]
                {{/linkTo}}
            </li>
        {{/each}}
    </ul>
</div>

<div class='small-9 columns'>
    {{outlet}}
</div>