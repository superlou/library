<div class='col-md-3'>
    <ul>
        {{#each controller}}
            <li>
                {{#linkTo 'checkouts.checkout' this}}
                    {{book.title}}, {{patron.name}} [{{status}}]
                {{/linkTo}}
            </li>
        {{/each}}
    </ul>
</div>

<div class='col-md-9'>
    {{outlet}}
</div>